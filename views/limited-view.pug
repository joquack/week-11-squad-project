extends layout.pug

block content
    if id
      p Welcome to #{name.charAt(0).toUpperCase() + name.slice(1)}'s Profile
    else
      a(href='/users/login') Login To See #{name.charAt(0).toUpperCase() + name.slice(1)}'s Profile
      p #{name.charAt(0).toUpperCase() + name.slice(1)}
include utils.pug


