doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title #{name.charAt(0).toUpperCase() + name.slice(1)}'s Profile | Stack Code
  body 
    p To see #{name.charAt(0).toUpperCase() + name.slice(1)}'s Profile you must be logged in!